import 'package:flutter/material.dart';

class PrivateCenter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        child: Text('个人中心'),
      ),
    );
  }
}
